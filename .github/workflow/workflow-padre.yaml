name: Parent Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/reusable-workflow.yaml
    with:
      some-input: 'sebastian'
    secrets:
      SOME_SECRET: ${{ secrets.SOME_SECRET }}